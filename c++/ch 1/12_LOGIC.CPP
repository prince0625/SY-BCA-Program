#include<iostream.h>
#include<conio.h>
void main()
{
	clrscr();
	int a=4 , b=5;
	cout<<(a>b && a<b); // and operator
        cout<<(a>b || a<b); // or operator
	cout<< !(a>b); // not operator
}