#include<iostream.h>
#include<conio.h>
void main()
{       clrscr();
	int a=8, b=3;
	cout<<"a+b="<<(a+b)<<endl; // addition
	cout<<"a-b="<<(a-b)<<endl; // substraction
	cout<<"a/b="<<(a/b)<<endl; // division
	cout<<"a*b="<<(a*b)<<endl; // multiplication
	cout<<"a%b="<<(a%b)<<endl; // modulation
}